import React from "react";

const HeroCard = ({
  title,
  description,
  image,
  className,
  isFullImage,
}: {
  title: string;
  description: string;
  image: string;
  className?: string;
  isFullImage?: boolean;
}) => {
  return <div>HeroCard</div>;
};

export default HeroCard;
